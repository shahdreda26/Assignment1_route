// Q1
    For each :بتتعامل مع ال array فقط , can make callback async ,مش بتقبل الbreak & continue & return
    for of :بتتعامل مع function & object & array& set & map بتقبل break & continue & return
    for of is faster than for each
    
// Q2
   Hosting : is behavior of moving declaration to top 
   var can declare after used it but let & const can not it 
   declaration function can declare after call it but expression function can not it 
  
    Temporal Dead Zone : is block area {...} & variable  is inaccessible until computer initializes of variable
     start at the beginning of block end when computer completely initialization وتعتبر انسب حاجه ل let &const         

// Q3 
    == is compared between value
    === is compared between value & datatype

// Q4 
   try (بحط فيها logic or function), handle error->catch (handle error if find)
   try-catch are block in async operations 
   try-catch are important : prevent crashes of app - code is readability and maintainability - enable error recovery 

// Q5
    conversion:include two type (Explicit , Implicit "coercion" ) and 
               convert from datatype into another datatype by built in function Number() , Boolean(),..
               Ex: let x = 0 
                   console.log(Boolean(x)) // false
    coercion  :convert from datatype into another datatype by manually 
               Ex: let y = "25"
               console.log(x + 5) //255 convert 5 from number into string
               